#include <bits/stdc++.h>
using namespace std;

#define Test

typedef long long ll;

class Solution {
public:
    long long maxProduct(vector<int>& nums) {
        ll ans = 0;
        sort(nums.begin(), nums.end());
        
    }
};
#include <bits/stdc++.h>
using namespace std;
#define Test

typedef long long ll;
class Solution {
public:
    long long bowlSubarrays(vector<int>& nums) {
        int n = nums.size(), l = 0, r = 2, max_val = nums[1];
        ll ans = 0;
        
    }
};

int main() {

    return 0;
}